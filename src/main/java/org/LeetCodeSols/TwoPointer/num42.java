package org.LeetCodeSols.TwoPointer;

/***
 * two pointers starting from each end, track the max height seen so far on each side
 * water trapped at a spot is the smaller of the two max heights minus the height there
 * move whichever side has the smaller max, that side is the one that still needs handling
 */

public class num42 {
    public int trap(int[] height) {
        int res = 0;




        return res;
    }
}
